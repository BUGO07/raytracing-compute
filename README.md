# Ray tracing - using a compute shader

## WIP

## Building and running

`cargo run --release` - builds and runs the project\
`cargo run --release -- cornell` runs the cornell box scene

You can tweak constants in assets/compute.wgsl